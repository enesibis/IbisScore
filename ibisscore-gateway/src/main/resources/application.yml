server:
  port: 8080

spring:
  application:
    name: ibisscore-gateway

  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "${CORS_ALLOWED_ORIGINS:http://localhost:3000}"
            allowedMethods: [GET, POST, PUT, DELETE, OPTIONS, PATCH]
            allowedHeaders: ["*"]
            allowCredentials: true
            maxAge: 3600

      routes:
        # ── User Service ──────────────────────────────────────────
        - id: user-service
          uri: ${USER_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/auth/**, /api/users/**
          filters:
            - name: CircuitBreaker
              args:
                name: user-service-cb
                fallbackUri: forward:/fallback/user

        # ── Match Service ─────────────────────────────────────────
        - id: match-service
          uri: ${MATCH_SERVICE_URL:http://localhost:8082}
          predicates:
            - Path=/api/fixtures/**, /api/leagues/**, /api/teams/**, /api/predictions/**
          filters:
            - name: CircuitBreaker
              args:
                name: match-service-cb
                fallbackUri: forward:/fallback/match

        # ── Betting Service ───────────────────────────────────────
        - id: betting-service
          uri: ${BETTING_SERVICE_URL:http://localhost:8084}
          predicates:
            - Path=/api/bets/**, /api/value-bets/**
          filters:
            - name: CircuitBreaker
              args:
                name: betting-service-cb
                fallbackUri: forward:/fallback/betting

      # default-filters: (rate limiter disabled for local dev)

  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}
      password: ${SPRING_REDIS_PASSWORD:}
      lettuce:
        client-options:
          protocol-version: RESP2

resilience4j:
  circuitbreaker:
    instances:
      user-service-cb:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
      match-service-cb:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
      betting-service-cb:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 10s

jwt:
  secret: ${JWT_SECRET}

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always
  health:
    redis:
      enabled: false

logging:
  level:
    com.ibisscore: DEBUG
    org.springframework.cloud.gateway: INFO
